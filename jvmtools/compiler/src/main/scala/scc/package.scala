package object scc {
  val WORKLO = "REGA" // in a chained expression the output of each subclause is placed here
  val TMP2 = "REGB" // local use var at very close scope
  val TMP1 = "REGC" // local use var at very close scope
  val WORKHI = "REGD" // in a chained expression the output of each subclause is placed here
}
